<resources>
    <!-- Window/splash theme only. The real UI colors come from Compose
         (see ui/Theme.kt), including dark mode and Material You dynamic color. -->
    <style name="Theme.VaultSend" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
